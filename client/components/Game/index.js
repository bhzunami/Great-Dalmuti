import Game from './Game';
import Join from './Join';
import New from './New';
import Play from './Play';

// all pages (for routing)
module.exports = {
  Game, // container page, this one wraps all other pages
  Join, // join game page
  New, // create new game page
  Play, // play game page
};
